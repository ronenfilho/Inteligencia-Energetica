# Exemplo de configuração do Airflow com Docker Compose
# Copie este arquivo para .env e preencha com suas credenciais reais

# Airflow Docker Image Configuration
AIRFLOW_IMAGE_NAME=apache/airflow:3.0.6
AIRFLOW_UID=50000
AIRFLOW_PROJ_DIR=.

# Airflow Web Interface Admin User
# Usuário e senha para acessar a interface web do Airflow
_AIRFLOW_WWW_USER_USERNAME=seu_usuario_admin
_AIRFLOW_WWW_USER_PASSWORD=sua_senha_forte_aqui

# Airflow Security Keys
# FERNET_KEY: Para gerar uma chave Fernet, execute: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=sua-chave-fernet-gerada-com-cryptography

# JWT_SECRET: Chave secreta para autenticação JWT na API do Airflow
AIRFLOW__API_AUTH__JWT_SECRET=sua-chave-secreta-muito-forte-e-aleatoria

# PostgreSQL Database Configuration
# Credenciais para o banco de dados PostgreSQL usado pelo Airflow
POSTGRES_USER=airflow
POSTGRES_PASSWORD=sua_senha_postgres_aqui
POSTGRES_DB=airflow

# Additional Python Requirements (optional)
# Adicione aqui pacotes Python adicionais separados por espaço
# Exemplo: _PIP_ADDITIONAL_REQUIREMENTS=pandas==1.5.0 requests==2.28.0
_PIP_ADDITIONAL_REQUIREMENTS=